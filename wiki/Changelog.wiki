#summary The three-pronged arrow of time.
#labels Featured,changes

== What's New in Version ... ==
* Three: Unused functions deleted; `fcalcmpx` split up; `megaunity` now several times faster; new `cornerstone.pl` parses Genbank search results; no more fudging with free2bind